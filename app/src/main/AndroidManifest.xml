<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- To install the downloaded update APK on API 26+ -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Needed to write firmware files straight onto a USB drive. On
         Android 11+, MainActivity.requestManageStorage() requests
         MANAGE_EXTERNAL_STORAGE at launch; on older versions it requests
         the two below instead. Without declaring them here, both requests
         are no-ops and every USB file write fails with EACCES. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- USB host so the projector exposes plugged-in drives -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Visibility into hidden factory/engineering menu packages (Android 11+) -->
    <queries>
        <package android:name="com.sstar.factorymenu" />
        <package android:name="com.sstar.systemcontrol" />
        <package android:name="com.sstar.customizedmenu" />
        <package android:name="com.newlink.projector.service" />
        <package android:name="com.newlink.factorymenu" />
        <package android:name="com.newlink.service" />
        <package android:name="com.zhiying.settings" />
        <package android:name="com.android.tv.settings" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ProjectorGuy">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:screenOrientation="landscape"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ProjectorGuy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Exposes the cached update APK to the system package installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
